# OAuth 클라이언트 ID 재생성 가이드 (redirect_uri_mismatch 해결)

## ❌ 현재 문제

`redirect_uri_mismatch` 오류가 발생합니다.
- 원인: "웹 애플리케이션" 타입으로 생성된 OAuth 클라이언트 ID
- 코드는 로컬 서버 기반 인증(`run_local_server`)을 사용하므로 "데스크톱 앱" 타입이 필요합니다

## ✅ 해결 방법: "데스크톱 앱" 타입으로 재생성

### 1단계: Google Cloud Console 접속

1. https://console.cloud.google.com/
2. 프로젝트 선택: `composite-dream-477907-c5`
3. APIs & Services → Credentials

### 2단계: 새 OAuth 클라이언트 ID 생성

1. **"자격 증명 만들기"** 클릭
2. **"OAuth 클라이언트 ID"** 선택

3. **애플리케이션 유형 선택**
   - ⭐ **"데스크톱 앱"** 선택 (중요!)
   - 한글판: "데스크톱 앱" 또는 "데스크톱 애플리케이션"

4. **이름 입력**
   - 이름: `3PL 솔루션 Desktop Client`

5. **"만들기"** 클릭

### 3단계: JSON 파일 다운로드 및 교체

1. **JSON 다운로드**
   - 생성된 클라이언트 ID 옆의 다운로드 아이콘 클릭
   - JSON 파일 다운로드

2. **파일 형식 확인**
   - 다운로드한 JSON 파일을 열어보면:
   ```json
   {
     "installed": {
       "client_id": "...",
       "project_id": "...",
       "auth_uri": "...",
       "token_uri": "...",
       "client_secret": "...",
       "redirect_uris": ["http://localhost"]
     }
   }
   ```
   - `web` 대신 `installed` 키를 사용합니다

3. **기존 credentials.json 교체**
   ```bash
   # 다운로드한 파일을 credentials.json으로 복사
   copy "다운로드한_파일명.json" "credentials.json"
   ```

### 4단계: 다시 테스트

1. **Flask 서버 실행**
   ```bash
   python app.py
   ```

2. **엑셀 업로드 테스트**
   - 브라우저: `http://localhost:5000/test-excel-upload.html`
   - 엑셀 파일 업로드 시도
   - 이제 Google 로그인이 정상적으로 작동해야 합니다!

## 🔍 확인 사항

- ✅ OAuth 클라이언트 ID 타입: "데스크톱 앱"
- ✅ credentials.json 파일에 `installed` 키가 있음
- ✅ `redirect_uri_mismatch` 오류가 더 이상 발생하지 않음

## 💡 참고

- "데스크톱 앱" 타입은 로컬 서버 기반 인증에 적합합니다
- "웹 애플리케이션" 타입은 웹 서버에서 직접 리디렉션을 처리할 때 사용합니다
- 현재 코드는 `run_local_server()`를 사용하므로 "데스크톱 앱"이 맞습니다

## ⚠️ 기존 클라이언트 ID

기존 "웹 애플리케이션" 타입의 클라이언트 ID는:
- 삭제해도 됩니다 (사용하지 않음)
- 또는 그대로 두어도 됩니다 (다른 용도로 사용 가능)

